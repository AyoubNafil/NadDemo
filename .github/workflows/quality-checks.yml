name: Quality Checks

on:
    push:
        branches:
            - '*'

jobs:
    quality-checks:
        name: 'Run Quality Checks'
        uses: 'AyoubNafil/devops-center-actions/blob/main/.github/workflows/quality-checks.yml@main'
